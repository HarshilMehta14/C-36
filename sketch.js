var myGameState = 0;
var myPlayerCount = 0;
var form,player,game;
var database, position;

function setup(){
    createCanvas(500,500);

    database = firebase.database();
   
}

function draw(){
    background("white");
    
}